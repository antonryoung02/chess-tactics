import { TacticContextParser } from "@utils";

describe("parseUciEvaluation", () => {
    test("", () => {
        const parser = new TacticContextParser();
    });
});

describe("parseMoveEvaluation", () => {
    test("", () => {
        const parser = new TacticContextParser();
    });
});

describe("parseEvaluation", () => {
    test("", () => {
        const parser = new TacticContextParser();
    });
});

describe("parse", () => {
    test("", () => {
        const parser = new TacticContextParser();
    });
});
